<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows plain HTTP only to local-dev hosts (emulator's host-machine alias
  and localhost) — the deployed backend uses HTTPS via Caddy/nginx per the
  roadmap's Phase 3 deployment plan, so this never applies to a real
  restaurant deployment; it exists purely so local development against a
  plain-HTTP backend doesn't need a throwaway TLS cert.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
