{
  "name": "@mydoners/customer-bot",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Customer-facing Telegram bot for @mydoner_bot — /start and /help outside the Mini App. The Mini App itself stays the only ordering surface.",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "grammy": "^1.31.0"
  },
  "devDependencies": {
    "bun-types": "^1.1.38",
    "typescript": "^5.7.2"
  }
}
