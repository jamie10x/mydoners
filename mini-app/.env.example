VITE_BACKEND_URL=http://localhost:3000

# Dev-only — used by src/devMockInitData.ts to sign fake launch params for
# testing in a regular browser outside Telegram. Must match the backend's
# TELEGRAM_BOT_TOKEN for local auth to succeed. Never used in production.
VITE_DEV_BOT_TOKEN=test-bot-token-12345
